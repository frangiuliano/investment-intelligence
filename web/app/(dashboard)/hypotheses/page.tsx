import { AreaPlaceholder } from "@/components/area-placeholder"

export default function HypothesesPage() {
  return (
    <AreaPlaceholder
      areaCode="02"
      title="Hypotheses"
      description="Track each thesis, its invalidation criteria, horizon, and eventual close without rewriting history."
    />
  )
}
